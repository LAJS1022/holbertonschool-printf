.TH _PRINTF 3 "November 2025" "Holberton School" "Library Functions Manual"

.SH NAME
_printf \- custom implementation of the printf function

.SH SYNOPSIS
.B #include "main.h"

.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf
function produces output according to a format string. It writes output to the standard output stream (stdout) using the
.B write
system call.

The format string is composed of zero or more directives. Ordinary characters are printed as-is. Format specifiers begin with a '%' character and are replaced with values from the variable argument list.

Supported format specifiers:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string of characters.

.TP
.B %%
Prints a literal percent sign.

.TP
.B %d, %i
Prints a signed decimal integer.

.SH RETURN VALUE
Returns the number of characters printed (excluding the null byte used to end output to strings). Returns
.B -1
if the format string is NULL or contains an incomplete format specifier (e.g. a lone '%').

.SH EXAMPLES
.nf
_printf("Hello, %s!\n", "Luis");
_printf("Score: %d%%\n", 100);
_printf("Character: [%c]\n", 'A');
.fi

.SH SEE ALSO
.BR printf (3),
.BR write (2),
.BR stdarg (3)

.SH AUTHOR
Written by Luis Jimenez for Holberton School :).

